/*
instance_create_layer(room_width/2, 0, "Instances", oSpirit)
var instance = instance_create_depth(room_width/2, 0, depth + 1, oSpirit);

with(instance){
	path_start(pth_spirit_try1, 10, path_action_stop, false);
}
